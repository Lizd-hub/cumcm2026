# 0.1.1 修订说明

基于初稿 0.1.0 另存，保留中文正文、四问推导与原生 Word 公式。

| 统一术语 | 定义与使用决定 |
|---|---|
| 旧改进策略 improved | 本轮配对基准；不与历史基础策略 baseline 混用 |
| 新策略 optimized | 当前默认求解器 |
| 保守多边形 | 正读数与先验形成的位置外包；负读数不直接裁剪第四问位置 |
| 自适应光学覆盖 | 对完整多边形递归分块，每块半径不超过 19.5 m |
| 平均每源虚拟耗时 | 先每局总虚拟时间除以清除数，再跨局等权平均 |

- 修正包围圆算法、接收感知前瞻、十轮测向、共享观测与分块收尾的描述，更新附录函数路径和运行命令。
- 主结果改为当前 76 场景、152 次配对运行，表格由 optimization_results.json 自动重建；六个源文件 SHA-256 均与实验记录一致。逐局结果和计时公式已核对。
- 删除不适用于自适应分块的旧 58 h 上界，改写为实际预算检查，明确几何完整性与限时成功的区别。
- 保留第一、二问证明及独立算例；图 7、图 8 和附录 C 明确标注为历史材料，避免与当前实验混算。保留官方测试未取得状态。
- 主文保留均值、P95、样本最大值与不利案例；逐场景细节集中于附录 A。替换过期段落，压缩重复收益结论，没有补造消融实验或显著性检验。

段落纯文本字符数（不含表格和原生公式）：15485 → 15667。表格增加来自独立种子及扩展误差场景的必要证据。

排版核验：经 Word 导出 25 页 PDF 并逐页检查页面图像，修正表格继承的首行缩进、列宽与跨页表头。公式（23）的比较对象已统一为 optimized / improved；图 1 明确标识历史流程及当前策略差异。原稿 0.1.0 未修改。
