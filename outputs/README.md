# 运行输出目录

该目录只保存可重新生成的运行产物，不纳入版本控制。常用子目录约定：

- `runs/`：单次离线或官方接口运行日志；
- `benchmarks/`：批量对比实验；
- `figures/`：重新生成的图表；
- `report/`：构建出的报告文件。

清理本目录不会影响源码和随仓库保存的基准结果。用于论文的固定基准数据仍位于
`src/b_solution/results/`，固定图像仍位于 `src/b_solution/figures/`。
